import { renderToStaticMarkup } from "react-dom/server";import{describe,expect,it}from"vitest";import{ExecutionReadiness}from"./execution-readiness";
describe("ExecutionReadiness",()=>{it("distinguishes recommendation, plan and disabled execution",()=>{const h=renderToStaticMarkup(<ExecutionReadiness plan={null} route={null}/>);expect(h).toContain("deterministic plan");expect(h).toContain("Execution not enabled")});it("shows blocked rule without executable route",()=>{const h=renderToStaticMarkup(<ExecutionReadiness plan={null} route={null} blockedReason="PROPOSAL_OUTSIDE_CONSTITUTION"/>);expect(h).toContain("Blocked");expect(h).toContain("No route is executable")})});
