import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it}from"vitest";import Docs from"./page";describe("public docs",()=>{it("renders authority status and truthful limits",()=>{const h=renderToStaticMarkup(<Docs/>);expect(h).toContain("Authority Model");expect(h).toContain("Public autonomous execution");expect(h).toContain("Disabled");expect(h).toContain('href="#faq"')})});
