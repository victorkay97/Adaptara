import{renderToStaticMarkup}from"react-dom/server";import{describe,expect,it}from"vitest";import Whitepaper from"./page";describe("whitepaper",()=>{it("renders thesis boundaries evidence and limitations",()=>{const h=renderToStaticMarkup(<Whitepaper/>);expect(h).toContain("Policy-Bounded");expect(h).toContain("bounded hands");expect(h).toContain("Fork Evidence");expect(h).toContain("not publicly deployed")})});
