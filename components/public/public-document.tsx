import Link from "next/link"; import { Brand } from "@/components/brand/brand";
export function PublicDocument({title,subtitle,sections}:{title:string;subtitle:string;sections:Array<{id:string;title:string;body:React.ReactNode}>}){return <main className="public-document"><header><Brand/><nav><Link href="/">Product</Link><Link href="/docs">Docs</Link><Link href="/whitepaper">Whitepaper</Link><Link href="/dashboard">Launch App</Link></nav></header><div className="public-document__layout"><aside><strong>{title}</strong>{sections.map(s=><a key={s.id} href={`#${s.id}`}>{s.title}</a>)}</aside><article><p className="eyebrow">Adaptara</p><h1>{title}</h1><p className="document-lede">{subtitle}</p>{sections.map(s=><section id={s.id} key={s.id}><h2>{s.title}</h2>{s.body}</section>)}</article></div></main>}
